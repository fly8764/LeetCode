###1.集合和元组的区别
####元组
1).元组不可修改元素，但可以通过 拼接来增加元组

####集合
1).增加元素；
set().add(x)：只能添加一个元素
set().update([1,3],[3,4])：可以添加多个元素，类参数表中的元素添加到原有set()中
2).删除元素
set().remove(x):删除指定元素，若不存在，则报错；
set().discard(x):同样删除指定元素，若不存在，不会报错


